# Data Layout

Put the avocado dataset metadata CSV in this folder or point the scripts to another location.

The file should follow `metadata_template.csv` and use paths that are either:

- absolute image paths
- relative paths resolved from the `--image-root` argument
